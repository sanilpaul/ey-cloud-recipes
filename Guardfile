# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard "foodcritic", cli: '--epic-fail any --chef-version 0.6' do
  watch(%r{attributes/.+\.rb$})
  watch(%r{providers/.+\.rb$})
  watch(%r{recipes/.+\.rb$})
  watch(%r{resources/.+\.rb$})
  watch(%r{README.md$})
end
